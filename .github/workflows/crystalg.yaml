name: crystalg

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal:0.34.0

    steps:
    - name: Get dependencies
      run: shards install
    - name: Run tests
      run: crystal spec
